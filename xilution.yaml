version: 1.0.0
dockerHub:
  account: xilution
  repository: custom-wordpress
tests:
  e2e:
    commands:
      - yarn install
      - bash $CODEBUILD_SRC_DIR_SourceCode/scripts/run-e2e-tests.sh $WORDPRESS_SITE_BASE_URL
  load:
    commands:
      - yarn install
      - bash $CODEBUILD_SRC_DIR_SourceCode/scripts/run-load-tests.sh $WORDPRESS_SITE_BASE_URL
